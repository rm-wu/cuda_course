name: cuda_11.8
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch
  - pytorch-cuda=11.8  # Note: 12.5 is not yet available in conda, 12.1 is latest stable
  - torchvision
  - cuda-toolkit=11.8.0
  - numpy
  - pandas
  - matplotlib
  - jupyter
  - pip
  - pip:
    - tensorboard

